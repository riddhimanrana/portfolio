// Screenshots referenced by building-lets-assist.md that were never added to
// the repo (broken on the previous Next.js site as well). Tracked to be fixed;
// remove entries here once the images land or the references are removed.
export const KNOWN_MISSING_BLOG_IMAGES = new Set<string>([]);
